import "Std.Overture"

# kwadraty liczb naturalnych od 1 do 20

class Main where
    static function main args =
        let nums = [1..20]
            list = List.replicate 20 (*) in 
            (list.zipWith (Function.call) nums).zipWith (Function.call) nums
