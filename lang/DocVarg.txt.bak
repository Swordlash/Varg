The Language Varg
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Varg==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.






UIdent literals are recognized by the regular expression
`````upper (letter | digit)*`````

LIdent literals are recognized by the regular expression
`````lower (letter | digit)*`````

MFun literals are recognized by the regular expression
`````'.' (letter | digit)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Varg are the following:
  | ``abstract`` | ``as`` | ``class`` | ``define``
  | ``deriving`` | ``else`` | ``function`` | ``has``
  | ``if`` | ``implement`` | ``implementing`` | ``import``
  | ``in`` | ``interface`` | ``internal`` | ``match``
  | ``module`` | ``public`` | ``sealed`` | ``static``
  | ``struct`` | ``super`` | ``template`` | ``then``
  | ``this`` | ``where`` | ``with`` | 

The symbols used in Varg are the following:
  | ; | ? | ( | )
  | [ | ] | , | :
  | { | } | = | final | unique
  | internal | unique | -> | \ | =>
  | == | < | > | <=
  | >= | + | - | *
  | / | ^ | _ | 

===Comments===
Single-line comments begin with #.Multiple-line comments are  enclosed with #/ and /#.

==The syntactic structure of Varg==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //ProgramDef// | -> | //[ImportDef]// //[ClassDef]// 
  | //[ClassDef]// | -> | **eps**
  |  |  **|**  | //ClassDef// //[ClassDef]// 
  | //ImportDef// | -> | ``import`` //UIdent// ``;`` 
  | //[ImportDef]// | -> | **eps**
  |  |  **|**  | //ImportDef// //[ImportDef]// 
  | //[ConstrTypeParam]// | -> | **eps**
  |  |  **|**  | //ConstrTypeParam// //[ConstrTypeParam]// 
  | //ConstrTypeParam// | -> | //LIdent// 
  |  |  **|**  | //UIdent// 
  |  |  **|**  | ``?`` 
  |  |  **|**  | ``(`` //TypeDef// ``)`` 
  |  |  **|**  | ``[`` //LIdent// ``super`` //[TypeDef]// ``]`` 
  |  |  **|**  | ``[`` ``?`` ``super`` //[TypeDef]// ``]`` 
  |  |  **|**  | ``[`` //LIdent// ``deriving`` //[TypeDef]// ``]`` 
  |  |  **|**  | ``[`` ``?`` ``deriving`` //[TypeDef]// ``]`` 
  | //TypeDef// | -> | //UIdent// //[ConstrTypeParam]// 
  |  |  **|**  | //LIdent// //[ConstrTypeParam]// 
  |  |  **|**  | //TypeDef1// 
  | //TypeDef1// | -> | ``(`` //TypeDef// ``)`` 
  | //[TypeDef]// | -> | //TypeDef// 
  |  |  **|**  | //TypeDef// ``,`` //[TypeDef]// 
  | //PrimFreeType// | -> | ``(`` //FreeTypeDef// ``)`` 
  |  |  **|**  | //LIdent// 
  |  |  **|**  | //UIdent// 
  | //FreeTypeDef// | -> | //UIdent// //[PrimFreeType]// 
  |  |  **|**  | //LIdent// //[PrimFreeType]// 
  |  |  **|**  | //FreeTypeDef1// 
  | //FreeTypeDef1// | -> | ``(`` //FreeTypeDef// ``)`` 
  | //[FreeTypeDef]// | -> | //FreeTypeDef// 
  |  |  **|**  | //FreeTypeDef// ``,`` //[FreeTypeDef]// 
  | //[PrimFreeType]// | -> | **eps**
  |  |  **|**  | //PrimFreeType// //[PrimFreeType]// 
  | //[ArgDef]// | -> | **eps**
  |  |  **|**  | //ArgDef// //[ArgDef]// 
  | //ArgDef// | -> | ``(`` //LIdent// ``:`` //TypeDef// ``)`` 
  |  |  **|**  | //LIdent// 
  | //SuperclassType// | -> | //FreeTypeDef// 
  | //[SuperclassType]// | -> | //SuperclassType// 
  |  |  **|**  | //SuperclassType// ``,`` //[SuperclassType]// 
  | //ClassDef// | -> | //[ClassModifier]// ``struct`` //UIdent// ``where`` ``{`` //[ClassField]// ``}`` 
  |  |  **|**  | //[ClassModifier]// ``class`` //UIdent// //IsDeriving// //IsImplementing// ``where`` ``{`` //ClassContents// ``}`` 
  |  |  **|**  | //[ClassModifier]// ``template`` //UIdent// //[ConstrTypeParam]// //IsDeriving// //IsImplementing// ``where`` ``{`` //ClassContents// ``}`` 
  | //IsImplementing// | -> | ``implementing`` //[SuperclassType]// 
  |  |  **|**  | **eps**
  | //IsDeriving// | -> | ``deriving`` //SuperclassType// 
  |  |  **|**  | **eps**
  | //[ClassModifier]// | -> | **eps**
  |  |  **|**  | //ClassModifier// //[ClassModifier]// 
  | //ClassModifier// | -> | ``module`` 
  |  |  **|**  | ``interface`` 
  |  |  **|**  | ``sealed`` 
  | //ClassContents// | -> | //[MemberDef]// //[FunDef]// 
  | //MemberDef// | -> | //UIdent// 
  |  |  **|**  | //UIdent// ``has`` ``{`` //[ClassField]// ``}`` 
  | //[MemberDef]// | -> | **eps**
  |  |  **|**  | //MemberDef// 
  |  |  **|**  | //MemberDef// ``;`` //[MemberDef]// 
  | //FunDef// | -> | //[FunctionModifier]// ``function`` //LIdent// //[ArgDef]// //RetType// ``=`` //Expr// 
  |  |  **|**  | //[FunctionModifier]// ``function`` //LIdent// //[ArgDef]// //AbsRetType// 
  | //RetType// | -> | ``:`` //FreeTypeDef// 
  |  |  **|**  | **eps**
  | //AbsRetType// | -> | ``:`` ``abstract`` //FreeTypeDef// 
  |  |  **|**  | **eps**
  | //[FunDef]// | -> | **eps**
  |  |  **|**  | //FunDef// 
  |  |  **|**  | //FunDef// ``;`` //[FunDef]// 
  | //[FunctionModifier]// | -> | **eps**
  |  |  **|**  | //FunctionModifier// //[FunctionModifier]// 
  | //FunctionModifier// | -> | ``static`` 
  |  |  **|**  | ``internal`` 
  |  |  **|**  | ``implement`` 
  |  |  **|**  | ``final | unique`` 
  | //ClassField// | -> | //FieldModifier// //LIdent// ``:`` //FreeTypeDef// 
  |  |  **|**  | //LIdent// ``:`` //FreeTypeDef// 
  | //[ClassField]// | -> | **eps**
  |  |  **|**  | //ClassField// 
  |  |  **|**  | //ClassField// ``;`` //[ClassField]// 
  | //FieldModifier// | -> | ``public`` 
  |  |  **|**  | ``internal | unique`` 
  | //Expr// | -> | ``define`` ``{`` //[AsDef]// ``}`` ``in`` //Expr// 
  |  |  **|**  | ``define`` //AsDef// ``in`` //Expr// 
  |  |  **|**  | ``match`` //Expr// ``with`` ``{`` //[MatchClause]// ``}`` 
  |  |  **|**  | ``if`` //Expr// ``then`` //Expr// ``else`` //Expr// 
  |  |  **|**  | ``(`` ``\`` //[ArgDef]// ``:`` //TypeDef// ``=>`` //Expr// ``)`` 
  |  |  **|**  | ``[`` //[ListElem]// ``]`` 
  |  |  **|**  | //Expr1// 
  | //[AsDef]// | -> | **eps**
  |  |  **|**  | //AsDef// 
  |  |  **|**  | //AsDef// ``;`` //[AsDef]// 
  | //AsDef// | -> | //Expr// ``as`` //LIdent// 
  | //[MatchClause]// | -> | **eps**
  |  |  **|**  | //MatchClause// 
  |  |  **|**  | //MatchClause// ``;`` //[MatchClause]// 
  | //MatchClause// | -> | //Expr// ``->`` //Expr// 
  | //Functorial// | -> | ``this`` 
  |  |  **|**  | ``super`` 
  |  |  **|**  | //UIdent// 
  |  |  **|**  | //LIdent// 
  |  |  **|**  | //MFun// 
  | //Arg// | -> | //Expr6// 
  |  |  **|**  | //Functorial// 
  | //[Arg]// | -> | **eps**
  |  |  **|**  | //Arg// //[Arg]// 
  | //Expr5// | -> | //Functorial// //[Arg]// 
  |  |  **|**  | //Expr6// 
  | //ListElem// | -> | //Expr// 
  | //[ListElem]// | -> | **eps**
  |  |  **|**  | //ListElem// 
  |  |  **|**  | //ListElem// ``,`` //[ListElem]// 
  | //Expr1// | -> | //Expr1// ``==`` //Expr2// 
  |  |  **|**  | //Expr2// ``<`` //Expr2// 
  |  |  **|**  | //Expr2// ``>`` //Expr2// 
  |  |  **|**  | //Expr2// ``<=`` //Expr2// 
  |  |  **|**  | //Expr2// ``>=`` //Expr2// 
  |  |  **|**  | //Expr2// 
  | //Expr2// | -> | //Expr2// ``+`` //Expr3// 
  |  |  **|**  | //Expr2// ``-`` //Expr3// 
  |  |  **|**  | //Expr3// 
  | //Expr3// | -> | //Expr3// ``*`` //Expr4// 
  |  |  **|**  | //Expr3// ``/`` //Expr4// 
  |  |  **|**  | //Expr4// 
  | //Expr4// | -> | //Expr5// ``^`` //Expr4// 
  |  |  **|**  | //Expr5// 
  | //Expr6// | -> | //Integer// 
  |  |  **|**  | //Double// 
  |  |  **|**  | ``_`` 
  |  |  **|**  | ``(`` //Expr// ``)`` 


