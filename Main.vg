import "Std.Overture"
import "Std.Queue"

class Main where
    static function main args = 
        let nats = [1..] in (nats.take 10000).foldl (+) 0
        #let fibs = 0:1: fibs.zipWith (+) (fibs.tail) in fibs.take 9
