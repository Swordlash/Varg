import "Std.Overture"
import "Std.Queue"

class Main where
    static function f (x:Int) : Int = if x == 0 then 0 else Main.g (x-1)
    static function g (x:Int) : Int = Main.f x
    
    static function main args = [1,2]
        #let nats = 1: nats.map (^incr) in nats.take 100
        #let fibs = 0:1: fibs.zipWith (+) (fibs.tail) in fibs.take 20
        #let ones = 1:ones in ones.take 10
